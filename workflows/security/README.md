# Security Workflow

This folder contains the security checks for the app.

Goals:

- rerun our baseline testing checks
- scan backend, DB API, and dashboard dependencies
- support a daily scheduled security run

Entry script:

- `workflows/security/run-security.sh`
